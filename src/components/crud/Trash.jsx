import React, { useEffect } from "react";
import Swal from "sweetalert2";

const Trash = ({ name }) => {
  useEffect(() => {}, []);
  return <div>{/* <h3>¿Seguro que quieres eliminar este usuario?</h3> */}</div>;
};

export default Trash;
